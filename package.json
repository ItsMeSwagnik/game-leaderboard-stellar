{
  "name": "stellar-rank-game-leaderboard",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "frontend/game_leaderboard"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend --if-present",
    "build": "npm run build --workspace=frontend --if-present",
    "preview": "npm run preview --workspace=frontend --if-present"
  },
  "dependencies": {}
}
