{
  "name": "stellar-rank-game-leaderboard",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend/app",
    "frontend/packages/game_leaderboard"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend/app",
    "build": "npm run build --workspace=frontend/app",
    "preview": "npm run preview --workspace=frontend/app"
  },
  "dependencies": {
    "@creit.tech/stellar-wallets-kit": "^2.5.0",
    "@stellar/freighter-api": "^6.0.1",
    "@stellar/stellar-sdk": "^16.0.1"
  }
}
